import { SafeAreaView, Text } from "react-native";
import React from "react";

const Home = () => {
  return (
    <SafeAreaView className="bg-slate-800 flex justify-center items-center w-screen h-screen">
      <Text className="text-white font-noor text-5xl">دلبر</Text>
    </SafeAreaView>
  );
};

export default Home;
